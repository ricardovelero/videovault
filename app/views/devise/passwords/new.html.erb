<div class="min-h-screen flex items-center justify-center bg-gray-100 px-4">
  <div class="w-full max-w-md bg-white p-8 rounded-lg shadow-md">
    <h2 class="text-2xl font-bold mb-6 text-center"><%= t '.forgot_password_title' %></h2>

    <%= form_with scope: :user, url: password_path(:user), local: true do |form| %>
      <div class="mb-6">
        <%= form.label :email, class: "block text-gray-700 mb-1" %>
        <%= form.email_field :email, autofocus: true, class: "w-full px-4 py-2 border rounded-lg focus:outline-none focus:ring focus:border-blue-300" %>
      </div>

      <%= form.submit t('.send_instructions'), class: "w-full bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-lg" %>
    <% end %>

    <div class="text-center mt-4">
      <p class="text-sm text-gray-600"><%= t '.login_question' %>
        <%= link_to t('.login'), new_user_session_path, class: "text-blue-600 hover:underline" %>
      </p>
    </div>
  </div>
</div>