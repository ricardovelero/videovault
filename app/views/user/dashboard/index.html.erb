<h1 class="text-2xl font-bold mb-4">Tu Panel</h1>

<p class="mb-6">Hola, <%= current_user.email %> 👋</p>

<div class="mb-4 space-x-4">
  <%= link_to "Ver videos", user_videos_path, class: "bg-blue-600 text-white px-4 py-2 rounded" %>
  <%= link_to "Nueva invitación", new_user_invitation_path, class: "bg-green-600 text-white px-4 py-2 rounded" %>
  <%= link_to "Historial de invitaciones", user_invitations_path, class: "bg-gray-600 text-white px-4 py-2 rounded" %>
</div>

<h2 class="text-xl font-semibold mt-6 mb-2">Últimas invitaciones</h2>

<ul class="space-y-2">
  <% @invitations.each do |invitation| %>
    <li class="border p-4 rounded bg-white shadow">
      <p><strong>Cliente:</strong> <%= invitation.email %></p>
      <p><strong>Video:</strong> <%= invitation.video.title %></p>
      <p><%= link_to "Ver enlace", invitation.link, target: "_blank", class: "text-blue-600 underline" %></p>
    </li>
  <% end %>
</ul>